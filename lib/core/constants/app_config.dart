/// Configuración central de la aplicación.
/// Edita estos valores antes de publicar una nueva versión.
class AppConfig {
  AppConfig._();

  /// Email del administrador con acceso al panel.
  static const String allowedEmail = 'beto.cristobal.ro@gmail.com';

  /// Versión actual de la aplicación (debe coincidir con pubspec.yaml).
  static const String appVersion = '1.0.0';
}
